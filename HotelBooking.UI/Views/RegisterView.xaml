<UserControl x:Class="HotelBooking.UI.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel HorizontalAlignment="Center">
            <Label>Förnamn</Label>
            <TextBox Text="{Binding FirstName}" Width="200" Margin="5"/>
            <Label>Efternamn</Label>
            <TextBox Text="{Binding LastName}" Width="200" Margin="5"/>
            <Label>Adress</Label>
            <TextBox Text="{Binding Address}" Width="200" Margin="5"/>
            <Label>Telefonnummer</Label>
            <TextBox Text="{Binding PhoneNumber}" Width="200" Margin="5"/>
            <Label>Email</Label>
            <TextBox Text="{Binding Email}" Width="200" Margin="5"/>
            <Label>Lösenord</Label>
            <TextBox Text="{Binding Password}" Width="200" Margin="5"/>
            <Button Command="{Binding SaveUserCommand}" Width="100" Margin="5">Registrera</Button>
        </StackPanel>    
    </Grid>
</UserControl>
